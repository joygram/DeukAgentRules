// Auto-generated by DeukPack V2 (Multi-Protocol)
<% const shortName = enm.name.includes('::') ? enm.name.split('::').pop() : enm.name; %>
#[derive(Debug, Clone, Copy, PartialEq, Eq, Default)]
pub enum <%- shortName %> {
    #[default]
<% Object.entries(enm.values).forEach(([name, value]) => { %>
    <%- name %> = <%- value %>,
<% }); %>
}

impl From<i32> for <%- shortName %> {
    fn from(v: i32) -> Self {
        match v {
<% Object.entries(enm.values).forEach(([name, value]) => { %>
            <%- value %> => Self::<%- name %>,
<% }); %>
            _ => Self::<%- Object.keys(enm.values)[0] %>, // Fallback to first variant
        }
    }
}
